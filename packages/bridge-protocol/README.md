# Pondview Bridge Protocol

Shared schemas, types, and client helpers for the Pondview bridge runtime.

This package is published so `pondview` can depend on a normal npm version
instead of a workspace-only dependency.
