# @pondview/docs

VitePress documentation site for Pondview.

Root workspace commands delegate here:

```bash
bun run docs:dev
bun run docs:build
bun run docs:preview
```

From this package directly, use `bun run --cwd packages/pondview-docs <script>`.
